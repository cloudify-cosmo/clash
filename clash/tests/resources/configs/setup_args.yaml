blueprint_path: ../blueprints/empty.yaml
name: setup_args
user_config_path: { env: USER_CONF_PATH }

setup:

  args:
    - name: arg1
    - name: --arg2
      required: true

  inputs:
    arg1: { arg: arg1 }
    arg2: { arg: arg2 }
