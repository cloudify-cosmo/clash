blueprint_path: ../blueprints/empty.yaml
name: after_setup
user_config_path: { env: USER_CONF_PATH }

setup:

  args:
    - name: arg1
    - name: arg2

  after_setup: workflowcmd.tests.commands.test_setup:after_setup
