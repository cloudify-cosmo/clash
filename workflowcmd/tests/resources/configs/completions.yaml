blueprint_path: ../blueprints/completions.yaml
name: completions

setup:

  args:
    - name: arg1
      completer: workflowcmd.tests.test_completion:setup_arg1_completer
    - name: --arg2
      completer: workflowcmd.tests.test_completion:setup_arg2_completer

commands:

  command1:
    args:
      - name: arg1
        completer: workflowcmd.tests.test_completion:user_arg1_completer
      - name: --arg2
        completer: workflowcmd.tests.test_completion:user_arg2_completer
    workflow: stub
